# Test-only paging cap; all other WorkspaceFiles fields keep their Config defaults.
- id: workspace-files
  name: '@deepseek-ai/dsh-api-workspace-files'
  config:
    maxLines: 64
# Pin desktop availability so the default-application controls render identically in headless CI.
- id: session-controller
  config:
    nativeOpen: true
