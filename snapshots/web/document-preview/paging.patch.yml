# Paging and unavailable-service guidance through the shipped Office viewer.
- id: workspace-files
  name: '@deepseek-ai/dsh-api-workspace-files'
  config:
    maxLines: 64
# Pin desktop availability so the default-application controls render identically in headless CI.
- id: session-controller
  config:
    nativeOpen: true
- id: office-to-pdf
  disabled: true
